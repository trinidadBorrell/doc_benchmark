# ── Python / tooling ──────────────────────────────────────────────────────
*__pycache__*
.ruff_cache/
.venv/
.venv_n/

# ── Editor / OS ───────────────────────────────────────────────────────────
.vscode/
.DS_Store

# ── Claude Code internals (not part of the release) ───────────────────────
.claude/
CLAUDE.md

# ── Run-time outputs and caches (re-created by the pipeline) ──────────────
results/
cv_splits/
data
notebooks/
narrative/

# ── Backups and scratch files kept locally only ───────────────────────────
cookbooks/pipeline.py.backup
src/model/txt/
src/report_completed_results.py
src/model/extratrees.py

# ── DK marker code: use the upstream `nice` library instead ───────────────
# https://github.com/nice-tools/nice — see README.md "Data" section.
src/markers/
